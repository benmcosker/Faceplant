[pytest]
# Put the backend/ dir (the package root for `app`) on sys.path so tests can
# `from app import ...` regardless of how pytest is invoked — bare `pytest`
# (as CI runs it) doesn't add the cwd the way `python -m pytest` does.
pythonpath = .
