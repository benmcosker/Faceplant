services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: faceplant
      POSTGRES_PASSWORD: faceplant
      POSTGRES_DB: faceplant
    ports:
      - "5433:5432"
    volumes:
      - faceplant_postgres_data:/var/lib/postgresql/data

volumes:
  faceplant_postgres_data:
